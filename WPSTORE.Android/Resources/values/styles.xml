<?xml version="1.0" encoding="utf-8" ?>
<resources>
  <!-- SplashActivity as launcher -->
  <style name="Theme.Splash" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowActionBar">false</item>
    <item name="android:windowFullscreen">true</item>
    <item name="android:windowContentOverlay">@null</item>
    <item name="android:windowBackground">@color/launcher_background</item>
  </style>

  <!-- MainActivity as launcher -->
  <!--<style name="Theme.Splash" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:windowBackground">@drawable/splash_screen</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowIsTranslucent">false</item>
    <item name="android:windowIsFloating">false</item>
    <item name="android:backgroundDimEnabled">true</item>
  </style>-->
  <style name="MainTheme" parent="MainTheme.Base">
  </style>
  <!-- Base theme applied no matter what API -->
  <style name="MainTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
    <!--If you are using revision 22.1 please use just windowNoTitle. Without android:-->
    <item name="windowNoTitle">true</item>
    <!--We will be using the toolbar so no need to show ActionBar-->
    <item name="windowActionBar">false</item>
    <!-- Set theme colors from https://aka.ms/material-colors -->
    <!-- colorPrimary is used for the default action bar background -->
    <item name="colorPrimary">#FFEA8025</item>
    <!-- colorPrimaryDark is used for the status bar -->
    <item name="colorPrimaryDark">#A0000000</item>
    <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
    <item name="colorAccent">#FFEA8025</item>
    <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight and colorSwitchThumbNormal. -->
    <item name="windowActionModeOverlay">true</item>

    <item name="android:textAllCaps">false</item>

    <!-- Used on ListView selected items -->
        <item name="android:colorPressedHighlight">@color/ListViewSelectedItemBackgroundColor</item>
        <item name="android:colorLongPressedHighlight">@color/ListViewSelectedItemBackgroundColor</item>
        <item name="android:colorFocusedHighlight">@color/ListViewSelectedItemBackgroundColor</item>
        <item name="android:colorActivatedHighlight">@color/ListViewSelectedItemBackgroundColor</item>
        <item name="android:activatedBackgroundIndicator">@color/ListViewSelectedItemBackgroundColor</item>
    
    <item name="android:datePickerDialogTheme">@style/AppCompatDialogStyle</item>
  </style>

  <style name="AppCompatDialogStyle" parent="Theme.AppCompat.Light.Dialog">
    <item name="colorAccent">#FFEA8025</item>
  </style>
</resources>
